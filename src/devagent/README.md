# DevAgent Package

This package contains all agents and the CLI interface for the DevAgent demo.

## Usage

Install dependencies:

```bash
pip install -r requirements.txt
```

Start the chat:

```bash
python -m devagent.main chat
```

Run health check:

```bash
python -m devagent.main health
```
